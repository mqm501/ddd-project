/**
 * aggregates - 聚合根
 * 聚合中可代表整个业务操作的实体对象，通过它提供对外访问操作，它维护聚合内部的数据一致性，它是聚合中对象的管理者，是这个聚合的根节点
 * 聚合软件包的根目录，根据聚合的名称命名，里面可以定义实体、聚合根、实体、值对象以及领域服务，可以独立拆分成微服务
 */
package com.mqm.demo.domain.ddd.model.aggregates;